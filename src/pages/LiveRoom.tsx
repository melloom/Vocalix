import { TikTokLiveRoom } from "@/components/TikTokLiveRoom";

// Re-export the TikTok-like LiveRoom component as the default
const LiveRoom = () => {
  return <TikTokLiveRoom />;
};

export default LiveRoom;
